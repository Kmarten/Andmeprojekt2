<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/enter_data_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="52dp"
        android:layout_marginTop="16dp"
        android:foregroundGravity="center_horizontal"
        android:hint="@string/enter_name_hint"
        android:maxWidth="180dp"
        android:minWidth="70dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/enter_data"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:foregroundGravity="center_horizontal"
        android:hint="@string/enter_data_hint"
        android:maxWidth="180dp"
        android:minWidth="70dp"
        app:layout_constraintStart_toStartOf="@+id/enter_data_name"
        app:layout_constraintTop_toBottomOf="@+id/enter_data_name" />

    <Button
        android:id="@+id/enter_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="40dp"
        android:layout_marginTop="20dp"
        android:text="Done"
        app:layout_constraintStart_toStartOf="@+id/enter_data"
        app:layout_constraintTop_toBottomOf="@+id/enter_data" />

</android.support.constraint.ConstraintLayout>